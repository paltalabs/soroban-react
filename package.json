{
  "name": "soroban-react",
  "scripts": {
    "build": "lerna run build",
    "lerna-publish": "lerna publish --no-private"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1"
  },
  "dependencies": {
    "lerna": "^6.4.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "0.0.0"
}
