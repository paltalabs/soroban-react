{
  "name": "soroban-react",
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish --no-private"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1"
  },
  "dependencies": {
    "lerna": "^6.4.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
