{
  "name": "soroban-react",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "lerna-publish": "lerna publish --no-private --conventional-commits",
    "lerna-publish-beta": "lerna publish --no-private --conventional-commits --dist-tag beta"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1"
  },
  "dependencies": {
    "lerna": "^6.4.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "0.0.0"
}
