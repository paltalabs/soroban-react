import type { InitOptions, Event as AnalyticsEvent, ReportConfig, AnalyticsOptions } from './types';
export declare class Analytics<T extends AnalyticsEvent> {
    private enabled?;
    private useQueue;
    private queue;
    private version;
    private app;
    private instanceId?;
    private sessionId?;
    private commitId?;
    private url?;
    private callbacks?;
    constructor({ version, app, useQueue }: AnalyticsOptions);
    init: (enabled: boolean | undefined, options: InitOptions) => void;
    enable: () => void;
    private flushQueue;
    disable: () => void;
    isEnabled: () => boolean;
    report: (data: T, config?: ReportConfig) => void;
}
//# sourceMappingURL=analytics.d.ts.map