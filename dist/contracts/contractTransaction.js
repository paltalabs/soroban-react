"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.contractTransaction = contractTransaction;
var StellarSdk = __importStar(require("@stellar/stellar-sdk"));
/**
 * Constructs a transaction to invoke a smart contract method call.
 * @param {ContractTransactionProps} props - Properties for building the transaction.
 * @returns {StellarSdk.Transaction} - The constructed transaction.
 */
function contractTransaction(_a) {
    var networkPassphrase = _a.networkPassphrase, source = _a.source, contractAddress = _a.contractAddress, method = _a.method, args = _a.args;
    var myParams = args || [];
    var contract = new StellarSdk.Contract(contractAddress);
    return new StellarSdk.TransactionBuilder(source, {
        // TODO: Figure out the fee
        fee: '100',
        networkPassphrase: networkPassphrase,
    })
        .addOperation(contract.call.apply(contract, __spreadArray([method], myParams, false)))
        .setTimeout(StellarSdk.TimeoutInfinite)
        .build();
}
